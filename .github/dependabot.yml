version: 2
updates:
  # Keep Dependabot security updates enabled via GitHub repository settings,
  # but disable routine version-update PRs for npm dependencies.
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    target-branch: "dev"
    open-pull-requests-limit: 0
