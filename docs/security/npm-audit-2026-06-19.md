# Weekly npm audit report — 2026-06-19

Repo: `TyMac/barista`  
Workdir: `/home/tmcadams/amplify-vite-react-template`  
Branch: `dev`

## Summary

Applied one minimal same-major dev-tooling update:

- `vite`: `~7.3.3` → `~7.3.5` (`node_modules/vite` resolved `7.3.3` → `7.3.5`)
- Lockfile side effect from clean resolution: `semver` `7.8.1` → `7.8.4`

Root audit counts improved from **39 total / 4 high / 1 critical** to **38 total / 3 high / 1 critical**.

## Audit counts

| Scope | Before | After |
| --- | --- | --- |
| root | 4 low, 30 moderate, 4 high, 1 critical, 39 total | 4 low, 30 moderate, 3 high, 1 critical, 38 total |
| `amplify/` | 1 moderate, 1 total | unchanged; no package changes in this scope |
| `amplify/function/gemini-api/` | 2 moderate, 2 total | unchanged; no package changes in this scope |

Raw audit JSON was saved under `/tmp/barista-audit-2026-06-19/` for this run.

## Findings considered and remediation decisions

### Patched

- **`vite` high advisories**
  - Baseline path: direct dev dependency `vite@7.3.3` plus peer references from `@tailwindcss/vite` and `@vitejs/plugin-react`.
  - Reachability: dev/build tooling. It may process local development/build server requests and build inputs; not a deployed runtime dependency.
  - Minimal safe remediation: same-major/same-minor patch update to `vite@7.3.5`, published more than 7 days before this run and installable without bypassing project npm protections.
  - Result: root audit high findings decreased by 1.

### Not patched / deferred

- **`fast-xml-parser` critical, `fast-xml-builder` high, and Amplify construct high findings**
  - Baseline paths: bundled/transitive copies under current Amplify Gen 2 backend packages such as `@aws-amplify/backend-data`, `@aws-amplify/data-construct`, `@aws-amplify/graphql-api-construct`, and AWS SDK credential/client internals. Some non-vulnerable newer `fast-xml-parser` copies also exist elsewhere in the tree.
  - Current direct Amplify backend packages checked: `@aws-amplify/backend@1.23.0` and `@aws-amplify/backend-cli@1.8.3` are already the current published versions available to this project during the run.
  - npm audit's suggested fix for the critical/high bundled findings points to `@aws-amplify/backend@1.2.1` as a semver-major/downgrade-style change and is not a safe Amplify Gen 2 remediation.
  - Overrides were not used because these vulnerable copies are bundled/internal and must not be claimed fixed unless the nested bundled copy actually changes; this run did not identify a safe override that changed the vulnerable nested copies without broad Amplify churn.
  - Reachability/residual risk: primarily backend deploy/build/control-plane tooling and AWS SDK XML parsing paths, not direct public web runtime code. Risk remains until upstream Amplify/AWS SDK packages publish compatible fixes.
  - Follow-up trigger: revisit when `@aws-amplify/backend`, `@aws-amplify/backend-cli`, `@aws-amplify/data-construct`, or related AWS SDK packages publish compatible versions that update the bundled vulnerable XML parser/builder copies.

## Local verification

- `npm install --save-dev vite@~7.3.5 --package-lock-only`: passed.
- `npm ci --cache .npm --prefer-offline`: passed.
- Removed local `.npm` cache after `npm ci`.
- `npm ls @aws-amplify/core --all`: passed; all visible Amplify core entries dedupe to `@aws-amplify/core@6.16.3`.
- `npm run build`: passed with Vite `7.3.5`; existing large chunk warning only.
- `graphify update .`: completed; graphify emitted pre-existing large-graph/field warnings but updated code graph artifacts.
- Post-update `npm audit --json`: 38 total / 3 high / 1 critical.

## Deploy and QA

- Commit: `ea49a302b154814f4345c30298e3133842acd4ff`
- Amplify dev job: `221` — `SUCCEED`
  - BUILD: `SUCCEED`
  - DEPLOY: `SUCCEED`
  - VERIFY: `SUCCEED`
- Anonymous browser QA: passed. `https://dev.barizta.ai` redirected to `https://www.dev.barizta.ai/`; the auth page visibly rendered with Sign In/Create Account tabs, social login buttons, email/password fields, and no blank-screen/broken-layout issue.
- Browser console after initial render: no JavaScript errors or console messages.
- Authenticated QA: skipped because the available browser automation interface would require placing the QA password directly in tool-call arguments/logs.
